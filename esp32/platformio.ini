[env:esp32dev]
platform  = espressif32
board     = esp32dev
framework = arduino

; Serial monitor baud rate
monitor_speed = 115200

; Libraries
lib_deps =
    olikraus/U8g2 @ ^2.35.30

; Build flags (optional — enables verbose BLE logging in serial monitor)
; build_flags = -DCORE_DEBUG_LEVEL=1
